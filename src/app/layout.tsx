import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TOSTEM India - Premium Aluminium Windows & Doors",
  description: "Tostem India brings Japanese precision engineering to aluminium windows and doors. 25+ years of legacy, 100+ colour options, 860+ cities served.",
  keywords: ["Tostem India", "Aluminium Windows", "Aluminium Doors", "Thermal Break", "Japanese Technology", "Sliding Windows", "Casement Windows"],
  icons: {
    icon: "/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
